import { DisableActionDirective } from './disable-action.directive';

describe('DisableActionDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableActionDirective();
    expect(directive).toBeTruthy();
  });
});
